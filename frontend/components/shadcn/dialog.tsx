"use client"

/**
 * Project Dialog primitive. Re-exports the shadcn Dialog so app code imports
 * from `@/components/shadcn/dialog` (per the wrapper convention) instead of the
 * raw `@/components/ui/dialog`. Add Rastro-specific variants here if needed.
 */
export * from "@/components/ui/dialog"
