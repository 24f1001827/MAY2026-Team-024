"use client"

/**
 * Project Spinner primitive. Re-exports the shadcn Spinner so app code imports
 * from `@/components/shadcn/spinner` (per the wrapper convention) instead of
 * the raw `@/components/ui/spinner`. Use it inline in a button to show which
 * action is in flight.
 */
export * from "@/components/ui/spinner"
