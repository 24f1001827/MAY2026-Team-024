"use client"

/**
 * Project Popover primitive. Re-exports the shadcn Popover so app code imports
 * from `@/components/shadcn/popover` (per the wrapper convention) instead of the
 * raw `@/components/ui/popover`. Add Rastro-specific variants here if needed.
 */
export * from "@/components/ui/popover"
