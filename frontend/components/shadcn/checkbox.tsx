"use client"

/**
 * Project Checkbox wrapper. Re-exports the shadcn Checkbox so app code imports
 * it from `@/components/shadcn/checkbox` instead of the raw `@/components/ui`
 * primitive. Add Rastro-specific variants here if they're ever needed.
 */
export * from "@/components/ui/checkbox"
